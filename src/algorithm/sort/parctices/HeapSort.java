package algorithm.sort.parctices;

import algorithm.sort.AbstractSort;

/**
 * @author AndyCui
 * @date 2018/9/27 下午10:35
 * @description 堆排序
 * 定义：当一棵二叉树的每个结点都大于等于它的两个子结点时，它被称为堆有序。相应的，每个结点都小于等于它的父结点。
 * 也就是说，从任意结点向上，能得到一列非递减的元素，从任意结点向下，能得到一列非递增的元素。可以得知，根结点
 * 是堆有序的二叉树的最大结点。
 * 二叉堆是一组能够使用堆有序的完全二叉树排序的元素，并在数组中按照层级存储（不使用数组的第一个位置）。
 */
public class HeapSort extends AbstractSort {
    @Override
    public void sort(Comparable[] c) {

    }
}
